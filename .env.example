# Base de datos (Aiven MySQL)
DB_HOST=
DB_PORT=3306
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_SSL=true

# JWT
JWT_SECRET=

# Passwords iniciales de usuarios (solo se usan la primera vez que arranca el servidor)
# Después los usuarios quedan en la tabla "usuarios" de la DB
ADMIN_PASS=
FABRICA_PASS=

# CORS — URL del frontend en Render
ALLOWED_ORIGINS=https://tu-frontend.onrender.com

# WhatsApp (UltraMsg) — opcional
ULTRAMSG_INSTANCE=
ULTRAMSG_TOKEN=
WHATSAPP_ADMIN=
